package com.google.firebase.auth;

import androidx.annotation.Keep;
import java.util.Arrays;
import java.util.List;

@Keep
public class FirebaseAuthRegistrar implements lb {
    @Keep
    public List<cb<?>> getComponents() {
        return Arrays.asList(new cb[]{cb.b(FirebaseAuth.class, cu.class).b(cg.g(gl.class)).f(u.a).e().d(), ax.a("fire-auth", "19.2.0")});
    }
}
